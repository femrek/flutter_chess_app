/// Enum for horizontal direction.
enum HorizontalDirection {
  /// New Pieces are added from the left side. the alignment is left.
  leftToRight,

  /// New Pieces are added from the right side. the alignment is right.
  rightToLeft,
}
